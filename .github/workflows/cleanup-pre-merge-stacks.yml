name: Clean up stale pre-merge deployments
run-name: Delete stale pre-merge deployments

on:
  workflow_dispatch:
  schedule:
    # Every weekday at 9am (10am is used by preview-cleanup - deletion rate limits)
    - cron: "0 9 * * 1-5"

permissions: {}

concurrency: cleanup-dev-${{ github.head_ref || github.ref_name }}

jobs:
  delete-stacks:
    name: Delete stale stacks
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    environment: development
    steps:
      - name: Assume AWS Role
        uses: aws-actions/configure-aws-credentials@99214aa6889fcddfa57764031d71add364327e59 # v6.1.3
        with:
          role-to-assume: ${{ secrets.AWS_PRE_MERGE_ROLE_ARN }}
          aws-region: eu-west-2

      - name: Get stale pre-merge stacks
        uses: govuk-one-login/github-actions/sam/get-stale-stacks@3b0015c58dd0e19e1572fda59dda4184df387d1e
        with:
          threshold-days: 1 # Pre-merge are short-lived test stacks
          stack-name-filter: pre-merge
          stack-tag-filters: |
            cri:component=ipv-cri-uk-passport-api
            cri:stack-type=pre-merge
            cri:application=Lime
            cri:deployment-source=github-actions
          env-var-name: STACKS

      - name: Delete stacks
        if: ${{ env.STACKS != null }}
        uses: govuk-one-login/github-actions/sam/delete-stacks@3b0015c58dd0e19e1572fda59dda4184df387d1e
        with:
          stack-names: ${{ env.STACKS }}
          verbose: true
